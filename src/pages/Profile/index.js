import React, { Fragment } from 'react';

import Navbar from '../../components/Navbar';

const Profile = () => (
  <Fragment>
    <Navbar />
    <h1>Profile</h1>
  </Fragment>
);

export default Profile;
